"use client";
import {useEffect,useState} from "react";
type Img={src:string;alt:string};
const projects=[{id:"der-leere-platz",title:"Der leere Platz",subtitle:"Bilderbuchprojekt",description:"Eine einfühlsame Geschichte über Einsamkeit, Zugehörigkeit und den Mut, aufeinander zuzugehen.",images:[1,2,3,4].map(n=>({src:`images/der-leere-platz/0${n}.jpg`,alt:`Der leere Platz – Illustration ${n}`}))},{id:"storchingo",title:"Storchingo",subtitle:"Bilderbuchprojekt",description:"Eine Geschichte über Anderssein, Identität und den Wunsch, dazuzugehören.",images:[1,2,3,4].map(n=>({src:`images/storchingo/0${n}.jpg`,alt:`Storchingo – Illustration ${n}`}))},{id:"ausgewaehlte-illustrationen",title:"Ausgewählte Illustrationen",subtitle:"Freie Arbeiten",description:"Eine kuratierte Auswahl weiterer Arbeiten aus Tinte, Aquarell und digitaler Nachbearbeitung.",images:[1,2,3,4,5,6].map(n=>({src:`images/ausgewaehlte-illustrationen/0${n}.jpg`,alt:`Ausgewählte Illustration ${n}`}))}];
export default function PortfolioGallery(){const[active,setActive]=useState<Img|null>(null);useEffect(()=>{const f=(e:KeyboardEvent)=>{if(e.key==="Escape")setActive(null)};window.addEventListener("keydown",f);return()=>window.removeEventListener("keydown",f)},[]);return <section id="portfolio" className="portfolio"><div className="portfolioIntro"><p className="sectionLabel">Portfolio</p><h2>Ausgewählte Bilderbucharbeiten</h2></div>{projects.map(p=><article className="project" id={p.id} key={p.id}><div className="projectHeading"><div><p className="sectionLabel">{p.subtitle}</p><h3>{p.title}</h3></div><p>{p.description}</p></div><div className="imageGrid">{p.images.map((im,i)=><button className="imageButton" key={im.src} onClick={()=>setActive(im)}><img src={im.src} alt={im.alt} loading={i>1?"lazy":"eager"}/></button>)}</div></article>)}{active&&<div className="lightbox" role="dialog" aria-modal="true" onClick={()=>setActive(null)}><button className="closeButton" onClick={()=>setActive(null)}>×</button><img src={active.src} alt={active.alt} onClick={e=>e.stopPropagation()}/></div>}</section>}
